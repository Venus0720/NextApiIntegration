'use client'
import React from 'react'

const ProducCard = () => {
  return (
    <div>
        <button onClick={() => console.log('click')}>
            Add to Card
        </button>
    </div>
  )
}

export default ProducCard